import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    image: {
        width: "100%",
        height: 500,
        resizeMode: "cover",
        alignItems: "center",
    },
    title: {

    }
});

export default styles;
